// == :--normal equal to sign [Value]
// === :--strict equal to sign ( value and type (string or integer))
// != :--Normal not equal to sign
// !== :-- Strict not equale(value and type not equale(same)) 

let a = 100
 let b  = "100"
console.log(a==b)
console.log(a!=b)

let x = 10
let y  = "10"
console.log(x===y) // check value and type of variable (so false)
console.log(x!==y) // (true):- value alag ho OR type alag ho Or (vice se vars)

